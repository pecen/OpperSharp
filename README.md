# OpperSharp
A C# SDK that wraps the functionality in the Opper API. 
It has basically the same set of methods as Opper's own Python SDK and TypeScript SDK. The idea came from since Opper only supports Python and TypeScript in their SDKs, I wanted to write a C# equivalence since I work mostly in C#. 
